{%- comment -%}
Legacy article template wrapper.
Note: No dedicated article sections were built; this renders a simple article view.
{%- endcomment -%}

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-10">
  <h1 class="text-4xl font-bold text-black mb-4">{{ article.title }}</h1>
  <p class="text-sm text-gray-500 mb-6">{{ article.published_at | date: "%b %d, %Y" }}</p>

  <div class="prose max-w-none">
    {{ article.content }}
  </div>
</div>
